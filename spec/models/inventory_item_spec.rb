require 'rails_helper'

RSpec.describe InventoryItem, type: :model do
  pending "add some examples to (or delete) #{__FILE__}"

  # it 'has a valid factory' do
  #   # Check that the factory we created is valid
  #   expect(build(:inventory_item)).to be_valid
  # end
  #
  # let(:attributes) do
  #     attrs = attributes_for(:inventory_item)
  # end
  #
  # let(:inventory_item) { create(:inventory_item, **attributes) }
  #   # let(:product) { create(:product) }
  #
  #   describe 'model validations' do
  #     # check that the name field received the right values
  #     it { expect(inventory_item).to allow_value(attributes[:item]).for(:item) }
  #     # ensure that the title field is never empty
  #     it { expect(inventory_item).to validate_presence_of(:item) }
  #
  #
  #     it { expect(inventory_item).to allow_value(attributes[:description]).for(:location) }
  #
  #     end
  #
  #     describe 'model associations' do
  #    # ensure a todo list has many items
  #    it { expect(inventory_item).to belongs_to(:item) }
  #    # it { expect(product).to have_many(:items) }
  #  end
  end
