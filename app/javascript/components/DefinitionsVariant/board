mutation updateVariant ($id: ID!,  $name: String!, $productId: ID!, $description: String!, $price: Float!, $size: String!, $picture: String!)){

  updateVariant(id: $id, name: $name, productId: $productId, description: $description, price: $price, size: $size, picture: $picture)
    {
    variant {
      id
      name
      description
    }
  }
}



<br/>
<VariantsList onVariantSelected={onVariantSelected} />
 Name: <br />
<input name="variantName" type="text" cols="30"
onChange={onVariantNameEntered}
></input> <br/>

Price: <br />
<input name="variantPrice" type="text" cols="30"
onChange={onVariantPriceEntered}
></input> <br/>


Description:<br />
<textarea name="description" rows="5" cols="30"
onChange={onVariantDescriptionEntered}> ></textarea><br/>


          <button className="Polaris-Button Polaris-Button--primary" onClick={e => {
              e.preventDefault();
              updateVar({ variables: { id: selectedVariant, name: variantName, productId:  parseInt(selectedProduct), description: variantDescription, price: parseFloat(variantPrice), size: "10", picture: " "} });
              window.location.reload();
            }}   type="submit">Update Variant</button>
