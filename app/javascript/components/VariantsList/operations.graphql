


query ItemsOfVariant ($id : Integer!){
  variant (id: $id) {
    id
    items {
      id
      name
    }
  }
}

query variants {
allVariants {
    id
    name
  }
}

query locations {
allLocations {
    id
    name
  }
}
